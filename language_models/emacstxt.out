emacs and its are a family of text that are by their
the manual for the most widely used emacs describes it as the display editor
development of the first emacs began in the and as of
emacs has over commands and allows the user to combine these commands into to work
emacs provides a deep extension capability allowing users and to write new commands using a dialect of the programming language
have been written to manage email files outlines and
the original emacs was written in by david a
moon and guy l
as a set of editor for the editor
it was inspired by the ideas of the and
the most popular and most version of emacs is emacs which was created by richard for the project
is a that branched from emacs in
both emacs and use emacs and are for the most part compatible with each other
emacs is along with one of the two main in the traditional editor of unix culture
both are among the oldest application programs still in use
